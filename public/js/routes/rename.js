// Generated by CoffeeScript 1.3.1
(function() {

  define(["smog/server", "templates/rename", "smog/notify"], function(server, templ, notify) {
    return function(_arg) {
      var name;
      name = _arg.name;
      $('#content').append(templ());
      $('#rename-modal').modal().css({
        'margin-left': function() {
          return -($(this).width() / 2);
        }
      });
      $('#rename-modal').on('hidden', function() {
        return $('#rename-modal').remove();
      });
      return $('#rename-button').click(function() {
        return server.collection({
          collection: name.toLowerCase(),
          type: 'rename',
          query: {
            name: $('#newname').val()
          }
        }, function(err) {
          if (err != null) {
            return notify.error("Error renaming collection: " + err);
          }
          $('#rename-modal').modal('hide');
          notify.success("Collection renamed");
          return window.location.hash = '#/home';
        });
      });
    };
  });

}).call(this);
