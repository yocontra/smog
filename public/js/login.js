// Generated by CoffeeScript 1.3.1
(function() {

  define(["smog/server", "templates/login", "smog/notify"], function(server, templ, notify) {
    return function() {
      $('#content').html(templ());
      return $('#login').on('submit', function(e) {
        var database, host, port;
        e.preventDefault();
        host = $('#host').val();
        port = parseInt($('#port').val());
        database = $('#database').val();
        return server.connect(host, port, database, function(err, okay) {
          if (err != null) {
            return notify.error("Connection error: " + err);
          } else {
            return notify.success('Connection established!');
          }
        });
      });
    };
  });

}).call(this);
